# Experiments

Committed experiment definitions and small reports belong here. Large logs, checkpoints, and run outputs belong under ignored `experiments/runs/` or an external content-addressed store.

Each report must identify code, runtime, model, dataset, tokenizer, configuration, hardware, metrics, tolerances, fallbacks, and retrieval checksums.
