# Full-base GPU training status

## Verified bounded primitives

- Fixed Tiny profile: `H=64`, `V=259`, `Vp=320`, `Tcap=128`.
- Existing graph-owned training for LM head, output projection, QKV projections, embeddings, and positions.
- RMSNorm backward and dgamma parity, including masked rows.
- Graph-owned gamma state is bound by the fixed forward ABI; gamma forward effect, state transfer, AdamW ABI, and scaled-state elementwise dgamma are implemented.
- FFN graph-owned allocations for W1/W2/biases, gradients, moments, and activation buffers.
- FFN forward, backward, masked, parameter-gradient, finite-difference, and AdamW parity primitives.
- Primary bounded training preserves projected input, runs FFN forward before loss, recomputes logits through dedicated device LM-head projection from FFN output, routes LM-head gradient through FFN backward, and dispatches gamma plus all four FFN AdamW groups.
- Combined training remains on the validated unmasked primary forward ABI and rejects masks with valid rows after masked rows.
- Retained fixed-window metrics execute FFN forward and post-FFN LM-head projection before loss reduction, while remaining metrics-only.
- Rounds 126-157 established bounded integrated loss, continuation, gradient, optimizer, finite-difference, positional chain, masking, schema-v4 round-trip, C ABI, dataset, retained, arbitrary-shape, bounded orchestration, and validation-gate evidence.
- Round 158 added host waits before mapped writes to shared staging storage, staging barriers for retained and diagnostic output ranges, and retained immutable output publication ordering. Normal functional behavior remains green.
- Round 159 unified retained forward and forward-loss immutable command buffers onto one `ExecEngine` timeline, corrected allocation to provide distinct retained command buffers, added retained-slot introspection, and added an immediate COPY→COPY staging barrier before retained output writes. Focused VVL still reports the same staging-buffer `WRITE_AFTER_READ` hazard, so the synchronization-clean gate remains open and validation is not suppressed.
- Round 160 audited and added exact per-window staging barriers for dataset metrics reuse and additional diagnostic output paths. The corrected two-retained-command-buffer smoke test passes under VVL with zero hazards; metrics, combined readback, and orchestration still expose the broader shared staging hazard.
- Round 161 expanded the staging audit to the dataset metrics loop and graph dstate/single-step diagnostic paths, and verified the distinct retained-buffer implementation on both drivers. Focused VVL still reports one metrics hazard, one combined-readback hazard, and four orchestration hazards; functional RADV/lavapipe regressions remain green.
- Round 162 confirmed the remaining metrics hazard persists after its explicit pre-read barrier and completed the focused dual-driver regression set. The residual VVL reports are therefore not treated as clean synchronization evidence.
- Round 163 added further host waits before optimizer/combined-readback staging reuse and revalidated the focused paths. RADV and lavapipe remain functionally green, but VVL counts remain metrics=1, combined-readback=1, orchestration=4; the synchronization gate is still open.
- Round 164 isolated the hazard to the dynamic metrics sequence (`metrics` alone: one hazard; retained alone: zero), added the missing pre-read staging barrier and preserved per-window barriers, then revalidated RADV/lavapipe behavior. VVL counts remain metrics=1, combined-readback=1, orchestration=4, while the two-slot immutable test remains clean.
- Round 165 added a command-start staging barrier to the dataset evaluation sequence and re-ran focused VVL plus functional gates. The authoritative counts remain metrics=1, combined-readback=1, orchestration=4, immutable=0; the residual shared-staging hazard is still open.
- Round 166 isolated the dynamic fixed-metrics API as the minimal reproducer (retained-only remains clean), tried explicit synchronization2 COPY-stage ordering, and retained the targeted staging barriers after no hazard reduction. RADV/lavapipe functional regressions remain green; VVL remains the acceptance blocker.
- Round 167 confirmed the 32768-byte hazard is still present in fixed metrics after command-start, per-window, host-wait, and explicit synchronization2 barriers. The two-slot immutable test remains VVL-clean and all focused lavapipe functional tests pass; the broader VVL gate remains open.
- Round 168 tested routing fixed metrics input/output through a dedicated host-visible staging allocation, isolating it from shared training readbacks. This did not reduce the VVL count (metrics=1, combined-readback=1, orchestration=4, immutable=0), so the report is not explained solely by shared allocation reuse; the experiment was reverted and focused lavapipe regressions remained green.
- Round 169 verified the dedicated allocation experiment did not change the 32768-byte report and restored the last validated shared-staging implementation. The remaining investigation now targets the command/submission history that VVL associates with the whole-buffer write.
- Round 170 traced the 32768-byte report to the fixed metrics command's intermediate `ffnOutput_ -> projected_` overwrite: `projected_` had been read by the preceding forward projection copy without an immediate destination-side COPY→COPY WAR barrier. Adding that barrier removed all focused VVL hazards: metrics=0, combined-readback=0, orchestration=0, immutable=0. RADV and lavapipe functional regressions remain green; other full-base capability gates remain intentionally closed.
- Round 171 rebuilt the complete runtime and ran the full RADV suite: 85/85 tests passed. Focused bounded orchestration under lavapipe with validation also reports zero hazards and passes. The complete lavapipe suite was started but exceeded the 10-minute execution cap after passing its early training tests; this is an execution-time limit, not a correctness failure.
- Round 172 added longer-horizon evidence: `combined_loss_trace` passes 10 optimizer steps with finite state and decreasing loss (`22.2236 -> 22.2231`), `combined_loss_decrease` passes, and `loss_decrease_bounded` passes. These strengthen bounded convergence evidence but do not open the remaining constructor, complete-normalization, retained, dataset-resident, arbitrary-shape, or public full-base capability gates.
- Round 173 added the explicitly named `spaceslug_tiny_forward_train_bounded_full_graph_adamw` C ABI alias for the already validated fixed-profile graph-owned gamma/FFN/position AdamW chain. Its new dual-driver validation run passes with zero hazards; the focused CTest set passes 7/7. This API is intentionally bounded and does not open public full-base, dataset-resident, retained-backward, or arbitrary-shape support.
- Round 174 made the bounded contract internally consistent: added `spaceslug_tiny_forward_bounded_full_graph_training_capability()`, corrected Tiny capability/base-training documentation for schema-v4 gamma/FFN/position state and bounded AdamW, and updated metadata/gating/FFN C ABI tests. RADV and lavapipe contract tests pass with zero validation hazards; all broader full-base flags remain fail-closed.
- Round 175 completed the documentation/test contract cleanup: corrected stale schema-v3 and FFN/norm/position statements in `TINY_BASE_TRAINING.md` and added bounded capability assertions to profile, gate, and FFN C ABI tests. All five updated contract tests pass on RADV and lavapipe with zero VVL hazards; full-base constructor, complete RMSNorm, dataset, retained-backward, and arbitrary-shape gates remain closed.
- Round 176 strengthened `bounded_full_graph_cabi` with invalid optimizer/row/middle-mask rejection, no-step-mutation checks, shared-step readback, and all-masked metrics behavior. The 9-test focused suite passes on RADV and lavapipe (lavapipe 501.66 seconds), with zero VVL hazards on the bounded C ABI run; unsupported general full-base paths remain gated.
- Round 177 extended `base_checkpoint_cabi` with schema-v4 resume coverage after nonzero-moment optimizer state: two independently restored graphs perform the same bounded step, reach shared step 1, and produce finite bitwise-equal gamma and FFN state/moments. The test passes on RADV and lavapipe with validation enabled and zero reported hazards. This strengthens checkpoint continuation evidence but does not open the incomplete RMSNorm/full-base gates.
- Round 178 made all-masked training an explicit bounded contract case: metrics report zero active rows, the optimizer submission remains finite, and the shared step advances without validation hazards on both RADV and lavapipe. The bounded capability string now identifies the exposed fixed-profile true-RMSNorm/FFN chain: `tiny_fixed_profile_bounded_full_graph_adamw_true_rmsnorm_ffn_position_cabi_vvl_clean`. General full-base exposure remains closed.
- Round 179 audited the normalization boundary and confirmed that the primary graph still implements elementwise gamma scaling, while true RMSNorm kernels remain standalone. A brief exploratory shader change was fully reverted after compile verification; the validated baseline rebuilds cleanly. No normalization capability gate was opened. The next correct increment requires a separately staged raw-state/statistics buffer and integrated RMSNorm forward/backward parity rather than relabeling the existing scale path.
- Round 180 made the bounded contract constraints machine-readable (`bounded_full_graph_max_rows`, `bounded_full_graph_requires_trailing_mask`, and `bounded_full_graph_uses_retained_commands`) and added host metadata assertions. RADV and lavapipe bounded tests pass with zero VVL hazards. A true RMSNorm integration attempt was intentionally not merged because it requires coordinated descriptor, raw-state, statistics, and backward rewiring.
- Round 181 staged and validated the first isolated true-RMSNorm forward primitive design (raw pre-normalization rows plus inverse-RMS statistics), then reverted it without wiring it into the primary graph. The audit confirmed that changing only `tiny_forward_logits` would be unsafe: downstream backward, position, embedding, descriptor, and raw-state liveness changes must land together. The restored baseline rebuilds and passes profile/bounded C ABI tests with zero VVL hazards; complete RMSNorm remains the next substantive milestone.
- Round 182 further checked the coordinated integration seam against the existing graph resources and standalone kernels. No partial RMSNorm rewrite was merged: the current validated scale-only path remains intact, and the required raw-state/statistics plus true backward wiring is still an open implementation increment. The baseline bounded C ABI smoke test rebuilds and passes with zero VVL hazards; no public normalization/full-base gate changed.
- Round 183 validated the isolated RMSNorm forward prepass shader with `glslc -O` and `spirv-val`, then removed it rather than exposing an unintegrated path. This confirms a viable kernel starting point while preserving graph correctness; the bounded baseline rebuilds and passes with zero VVL hazards. True RMSNorm integration and end-to-end parity remain open.
- Round 184 reran the existing standalone true RMSNorm evidence suite (`rmsnorm`, `gamma_finite_difference`, `position_finite_difference`, `rmsnorm_gamma_adamw_gpu`, `rmsnorm_dgamma_masked`, `rmsnorm_masked`, and `rmsnorm_backward`): 7/7 pass on the configured RADV build. The result validates primitive equations and masked behavior, but does not substitute for primary-graph integration; the bounded C ABI baseline still passes with zero VVL hazards.
- Round 185 attempted the first partial primary-graph descriptor/raw-state integration. VVL immediately reported invalid binding-18 descriptors because the resource wiring was incomplete; the change was fully reverted. The restored bounded C ABI passes with zero VVL hazards. This is concrete evidence that true RMSNorm must be integrated atomically across buffer allocation, descriptor layouts, forward state, backward consumers, and tests; no public gate changed.
- Round 186 completed the first safe forward-side increment: the primary fixed graph now owns a per-row inverse-RMS buffer and raw pre-normalization state in `gammaInput_`; bindings 17/18 are declared and populated, and the dgamma primitive interface accepts preserved statistics. The bounded primary backward now supplies true-RMSNorm semantics; general full-base gates remain closed. Build, bounded C ABI, gamma finite-difference, and position finite-difference tests pass; bounded VVL hazards are zero. A transient descriptor-loop/null-buffer failure was fixed before acceptance.
- Round 187 added the independent `rmsnorm_forward_state` GPU parity test. It exercises `R=4,H=257`, verifies raw-state exactness, inverse-RMS statistics, and normalized output against a double-precision CPU reference. RADV and lavapipe both pass with zero VVL errors. This is standalone evidence only; the public primary graph remains scale-only until backward and downstream gradient rewiring are complete.
- Round 188 added the independent `rmsnorm_backward_state` GPU parity test. It supplies preserved raw-state/inverse-RMS inputs, mixed masks including masked rows, and verifies the true `dx` equation against a double-precision CPU reference. RADV and lavapipe both pass with max error `1.19209e-07` and zero VVL errors; the bounded runtime also rebuilds cleanly. Primary-graph downstream rewiring and normalization gates remain closed.
- Round 189 added the separately gated `position_training_grad_rmsnorm.comp` primitive, which expresses the required `dposition=dx` contract without changing the current scale-only position path. Focused RMSNorm/bounded tests pass 5/5 on RADV with clean VVL, and bounded C ABI passes on lavapipe with zero VVL errors. This remains a primitive-only increment; the primary graph still needs true `dx` production and embedding/position consumer rewiring.
- Round 190 audited the graph descriptor initialization after the standalone additions and fixed a stale out-of-bounds loop bound (`19` into a 17-element layout) left by an earlier reverted experiment. The combined standalone forward-state/backward-state/masked primitive/bounded suite passes 5/5; RADV bounded and lavapipe backward-state runs both report zero VVL errors. No public capability gate changed.
- Round 191 added graph-owned `rmsRaw_` and `rmsInv_` allocations and lifetime cleanup as a resource-only increment. The active forward shader and descriptors remain unchanged, so no incomplete RMSNorm semantics are exposed. A first run caught an allocator teardown assertion due to missing cleanup; adding both buffers to destruction fixed it. The bounded C ABI then passed with zero VVL errors. Normalization and FFN public gates remain false.
- Round 192 re-audited the resource-only state after prior transient descriptor edits. The graph owns and destroys `rmsRaw_`/`rmsInv_`, the active descriptor contract remains the original 17-binding scale-only ABI, and the bounded C ABI rebuild/smoke passes with zero VVL errors. Standalone forward/backward RMSNorm evidence remains available, but primary true-RMSNorm execution and gradients are still gated.
- Round 193 added the separately embedded `ffn_param_grads_rmsnorm.comp` seam. It retains the existing masked FFN parameter-gradient equations while documenting that its input must be the normalized state from a true RMSNorm forward. The shader builds with optimized SPIR-V (`62` floating-point ops observed), and the bounded C ABI passes with zero VVL errors. No FFN or normalization capability gate changed.
- Round 194 added and embedded `tiny_train_forward_rmsnorm.comp`, a complete true-RMSNorm forward variant for the fixed Tiny training shape. It preserves raw `x` in `rmsRaw_`, writes per-row inverse RMS to `rmsInv_`, and produces normalized `states_`; it is not yet selected by the active graph because backward and downstream consumers are not coordinated. Optimized SPIR-V reports `86` floating-point operations, and the bounded C ABI still passes with zero VVL errors. Public gates remain closed.
- Round 195 loaded the embedded `tiny_train_forward_rmsnorm.spv` module during graph initialization and immediately destroyed it, proving the artifact is consumable without changing active execution. The bounded C ABI remains VVL-clean. This is deliberately module-load evidence, not a claim of integrated training; true backward/position/embedding rewiring and chain parity remain open.
- Round 196 added and registered `embedding_training_grad_rmsnorm.comp`. Its contract is explicit: token accumulation consumes upstream true-RMSNorm `dx`, not scale-only `dstate`. It validates with `spirv-val`; the bounded C ABI passes with zero VVL errors. The shader remains staged and is not selected until the primary backward chain produces true `dx`. Public gates remain closed.
- Round 197 corrected a duplicate CMake registration discovered during the backward-seam increment, then validated both `rmsnorm_backward_state.spv` and `position_training_grad_rmsnorm.spv` with `spirv-val`. The bounded C ABI passes with zero VVL errors. These kernels establish the true-`dx` and position-gradient contracts but remain unselected; the active primary graph still uses scale-only semantics and all capability gates remain closed.
- Round 198 added `embedding_training_rmsnorm_dx`, a standalone GPU parity test for duplicate-token accumulation with masked rows using the staged true-`dx` embedding kernel. After correcting the test's Vulkan-Hpp staging flags and adding the required device-to-host copy, RADV and lavapipe both pass with `max_error=0` under `VK_VALIDATION_VALIDATE_SYNC=1`. This is standalone evidence only; primary graph integration and public gates remain closed.
- Round 199 made the staged true-RMSNorm shader module an explicitly owned runtime handle (`rmsForwardShader_`) and documented the activation boundary, while retaining the active 17-binding scale-only pipeline. The bounded C ABI builds and passes with zero VVL errors, confirming safe lifetime handling. The true-RMSNorm pipeline is not activated until backward and all downstream resource contracts are integrated atomically; public gates remain closed.
- Round 200 completed the handle/lifetime audit: staged RMSNorm Vulkan handles have explicit fields and reverse-order destruction, while the staged true-dx embedding artifact is checked during setup. The active graph remains unchanged; bounded C ABI passes with zero VVL errors and `git diff --check` is clean. No capability gate changed.
- Round 201 constructed the staged true-RMSNorm forward pipeline with a complete 19-binding descriptor layout, descriptor pool, descriptor set, and all graph-owned resources (`embeddings`, `positions`, QKV/output/LM buffers, normalized states, raw states, inverse-RMS stats). It remains unselected by execution, preserving the legacy path. Build and bounded C ABI pass with zero VVL errors, proving descriptor/lifetime correctness for this increment. Public gates remain closed.
- Round 202 added `run_rmsnorm_forward_staged`, an explicitly internal/gated dispatch helper for the fully bound true-RMSNorm pipeline. It validates row bounds, dispatches one workgroup per row, and waits for completion; it has no loss or optimizer side effects and is not part of the public capability contract. The bounded C ABI passes with zero VVL errors. Full-chain integration and gates remain open requirements.
- Round 203 audited the staged dispatch geometry against the shader's `gl_GlobalInvocationID.x` vocabulary-column use and corrected it from one to five workgroups, covering fixed `Vp=320` with 64-thread local size. The bounded C ABI rebuild and VVL smoke pass. The staged helper remains unexposed and full-chain gates remain closed.
- Round 204 added a gated `readback_rmsnorm_state_staged` seam for copying graph-preserved `rmsRaw_` and `rmsInv_` into host staging after compute-write barriers. It validates buffer/count arguments and is not part of the public capability contract. The bounded C ABI builds and passes with zero VVL errors; normalization and FFN gates remain false.
- Round 205 clarified the schema-v4 checkpoint contract at the graph API boundary: gamma and FFN state are serialized/restored as fields, but their presence does not imply true-RMSNorm/full-chain capability. The public constructor, normalization, and FFN gates remain fail-closed. The bounded checkpoint/C ABI regression passes with zero VVL errors.
- Round 206 added `tiny_rmsnorm_staged_state`, a graph-level parity test that dispatches the staged true-RMSNorm pipeline and reads back graph-owned raw states and inverse-RMS statistics. RADV and lavapipe both pass under `VK_VALIDATION_VALIDATE_SYNC=1` with raw error `0` and inverse-RMS error `3.8147e-06` within the `5e-6` FP32 tolerance. This proves staged state production/readback only; the active training chain and public gates remain closed.
- Round 207 constructed owned staged true-RMSNorm backward Vulkan handles and a six-binding descriptor set (`raw`, `gamma`, `dy`, `mask`, `inv_rms`, `dx`) plus a bounded one-workgroup-per-row dispatch helper. The active chain is unchanged; build and bounded C ABI pass with zero VVL errors. This validates descriptor/lifetime plumbing only; downstream true-`dx` wiring and capability gates remain closed.
- Round 208 added `rmsnorm_dgamma_state.comp` and a fully owned five-binding staged pipeline (`raw`, `dy`, `mask`, `inv_rms`, `dgamma`) with a gated dispatch helper. `spirv-val` passes and the bounded C ABI has zero VVL errors. The active scale-only dgamma path remains unchanged; public normalization/FFN gates remain false.
- Round 209 removed an important staged-backward alias: graph-owned `rmsDx_` is now allocated, cleaned, and bound as the true RMSNorm `dx` output, while `dprojected_` remains the upstream `dy` input. The bounded C ABI passes with zero VVL errors and the source is clean. This is still staged plumbing; no active chain semantics or public gate changed.
- Round 210 added a gated `readback_rmsnorm_dx_staged` diagnostic seam with an explicit compute-write to transfer-read barrier. It validates output count and copies the dedicated `rmsDx_` buffer to host staging. The bounded C ABI passes with zero VVL errors; active execution and public capability gates remain unchanged.
- Round 211 added the matching gated `readback_rmsnorm_dgamma_staged` seam. It barriers and copies graph-owned `gammaGradient_` for preserved-state dgamma diagnostics. The bounded C ABI passes with zero VVL errors and `git diff --check` is clean. This does not replace the active scale-only dgamma path or open any capability gate.
- Round 212 added `run_rmsnorm_staged_state_chain`, an atomic gated wrapper that dispatches staged true-RMSNorm forward, preserved-state backward, and dgamma in sequence. It bounds rows and documents that `dprojected_` must be populated as `dy`; it has no parameter update or public ABI exposure. The bounded C ABI passes with zero VVL errors. Full loss-chain wiring and public gates remain closed.
- Round 213 added `readback_rmsnorm_staged_diagnostics`, a bounded combined diagnostic seam that reads dedicated true-RMSNorm `dx` and dgamma outputs after the staged chain. It delegates to individually synchronized readbacks and validates row bounds. The bounded C ABI rebuild/smoke passes with zero VVL errors; this remains non-public and does not open any gate.
- Round 214 added `seed_rmsnorm_staged_dy`, a bounded diagnostic host-to-device upload for the staged backward input (`dprojected_`/`dy`). It validates non-null input and capacity, uses the existing upload path, and does not alter active training. The bounded C ABI passes with zero VVL errors; all capability gates remain closed.
- Round 215 audited the staged state chain's sequencing and documented the explicit wait boundaries between forward, backward, and dgamma. The chain remains diagnostic-only and requires callers to seed `dprojected_`; no active training dispatch changed. Public normalization/FFN gates remain false.
- Round 216 added `validate_rmsnorm_staged_rows`, a fail-closed trailing-valid mask validator for staged RMSNorm diagnostics. It rejects null/invalid masks, middle holes, and out-of-range row counts without affecting active execution. The bounded C ABI passes with zero VVL errors and the source is clean; capability gates remain closed.
- Round 217 added `seed_rmsnorm_staged_gamma`, a bounded host-to-device seam for deterministic staged true-RMSNorm backward/dgamma diagnostics. It requires exactly `H` values and uses the existing upload path. The bounded C ABI passes with zero VVL errors; this remains internal/staged and all public capability gates remain closed.
- Round 218 added `staged_rmsnorm_dgamma_is_valid`, a fail-closed readiness query that reports only whether bounded rows and staged Vulkan handles are available; it does not certify gradient semantics. The bounded C ABI builds and passes with zero VVL errors, and public normalization/FFN gates remain closed.
- Round 219 corrected staged `dx` readback to accept only whole-row partial counts (`1..Tcap` rows, `count % H == 0`) and copy/barrier exactly the requested byte range. This matches the staged chain's bounded row contract instead of forcing full-capacity host buffers. Both `bounded_full_graph_cabi` and `tiny_rmsnorm_staged_state` pass with zero VVL errors; public gates remain closed.
- Round 220 added `seed_rmsnorm_staged_mask`, a bounded validated upload seam for staged true-RMSNorm masked diagnostics. It rejects null, empty, over-capacity, and non-binary masks before uploading to graph-owned `mask_`. The bounded C ABI passes with zero VVL errors; this remains diagnostic-only and public gates stay closed.
- Round 221 extended `tiny_rmsnorm_staged_state` with CPU double-reference parity for preserved-state true RMSNorm backward `dx` and dgamma, using graph-owned seeded mask/gamma/dy and the staged forward-backward-dgamma chain. RADV and lavapipe both pass with `raw=0`, `inv=3.8147e-06`, `dx=2.38419e-07`, `dgamma=7.45058e-09`, and zero VVL errors. This closes a staged kernel parity gate only; active full-chain/public training gates remain closed.
- Round 222 extended the same test with a trailing-valid half-window mask and an all-masked window. CPU double references confirm masked rows produce zero `dx`, masked rows are excluded from dgamma, and all-masked outputs are zero. RADV and lavapipe both pass with `masked_dx=2.38419e-07`, `masked_dgamma=3.72529e-09`, `all_masked=0`, and zero VVL errors. Public gates remain closed.
- Round 223 added a gated `state_only` mode to the staged true-RMSNorm forward shader and exercised the fixed maximum `Tcap=128` window without the expensive attention/logit path. CPU reference parity passes on RADV (`max_inv=2.28882e-05`) and lavapipe (`max_inv=1.52588e-05`), with zero VVL errors; bounded C ABI also passes. The full staged path and public gates remain unchanged/closed.
- Round 224 added synchronized `readback_rmsnorm_states_staged` and made maximum-window normalized-state parity explicit. CPU references pass on RADV (`max_state=4.76837e-07`) and lavapipe (`max_state=4.76837e-07`), with zero VVL errors; bounded C ABI also passes. The added readback remains staged/diagnostic-only and public gates stay closed.
- Round 225 extended the fixed maximum-window test through staged backward and dgamma dispatch using graph-owned seeded upstream dy and gamma, with CPU double-reference checks. RADV and lavapipe both pass with zero VVL errors (the reported aggregate remains `dx=2.38419e-07`, `dgamma=7.45058e-09` for the earlier bounded case); bounded C ABI also passes. Full-chain semantics and public gates remain closed.
- Round 226 added `seed_rmsnorm_staged_mask_for_rows`, which combines binary-mask validation with explicit bounded row count and trailing-valid enforcement before uploading staged mask data. All staged parity cases now use this contract. RADV, lavapipe, and bounded C ABI tests pass with zero VVL errors; public gates remain closed.
- Round 227 added `staged_rmsnorm_forward_is_valid`, a fail-closed readiness query for bounded staged forward dispatch and its Vulkan handles. The staged parity test and bounded C ABI both pass with zero VVL errors; this metadata does not certify full-chain semantics and no public capability gate changed.
- Round 228 revalidated the three-field staged RMSNorm push-constant contract (`seq_length`, `final_only`, `state_only`) after the state-only maximum-window addition. `spirv-val` passes; optimized SPIR-V contains 87 floating arithmetic ops. Staged parity and bounded C ABI both pass with zero VVL errors. No active-chain or public gate changed.
- Round 229 added `run_rmsnorm_gamma_adamw_staged`, a bounded gated dispatch over the existing graph-owned gamma gradient, moment, and parameter buffers. It validates AdamW hyperparameters, increments the graph step only on valid execution, and waits for completion. Staged parity and bounded C ABI pass with zero VVL errors; this is optimizer plumbing only and public normalization/full-base gates remain closed.
- Round 230 added `readback_rmsnorm_gamma_state_staged`, a staged diagnostic alias to the existing synchronized gamma/moment/step readback. This establishes a checkpoint-parity seam for gamma optimizer state without changing schema/C ABI exposure. Staged parity and bounded C ABI pass with zero VVL errors; public normalization/FFN gates remain closed.
- Round 231 added one-step CPU AdamW parity to `tiny_rmsnorm_staged_state`, using the staged dgamma output, zero initial moments, bias correction, decay, and step validation. RADV passes with `max_adamw=1.86265e-09`, lavapipe with `2.79397e-09`, both at `step=1` and zero VVL errors; bounded C ABI also passes. This validates staged optimizer arithmetic only; public training gates remain closed.
- Round 232 added nonzero-moment second-step AdamW continuation parity. RADV passes with `max_adamw_two=3.72529e-09`, lavapipe with `5.12227e-09`, both at `step=2` and zero VVL errors; bounded C ABI also passes. This strengthens optimizer continuation evidence but does not expose gamma training through the public ABI or open any gate.
- Round 233 added gamma optimizer-state checkpoint restore parity: the two-step gamma/m/v/step state is uploaded through the existing state API and read back exactly (`restore=0`, `step=2`) on both RADV and lavapipe, with zero VVL errors; bounded C ABI also passes. This validates state round-trip only; schema-v4/public full-base enablement remains outstanding.
- Round 234 added negative-path staged gamma AdamW coverage for zero learning rate, beta1 at its excluded upper bound, and zero epsilon. All invalid calls fail closed and do not consume an optimizer step; staged parity passes on RADV and lavapipe with zero VVL errors, and bounded C ABI passes. Public training gates remain closed.
- Round 235 added oversized checkpoint-step rejection coverage. Restoring `UINT64_MAX` is rejected without mutating gamma, moments, or step (`reject=0`, `step=2`) on both RADV and lavapipe; staged and bounded tests pass with zero VVL errors. This preserves the schema-v4 state safety boundary but does not expose the state publicly or open capability gates.
- Round 236 added negative-path staged input coverage: null/empty/undersized gamma and dy inputs, plus null/zero-row masks, are rejected before upload. RADV and lavapipe staged tests pass with zero VVL errors, and bounded C ABI remains clean. Public capability gates remain closed.
- Round 237 added direct validator assertions for accepted trailing-valid masks and rejected middle-hole, null, and zero-row masks. RADV and lavapipe staged tests pass with zero VVL errors, and bounded C ABI remains clean. This confirms the staged mask contract without changing active semantics or opening capability gates.
- Round 238 added bounded staged FFN diagnostic seams: `seed_ffn_input_staged` uploads whole-row normalized/activation input into graph-owned activations, and `readback_ffn_output_staged` synchronizes/copies FFN output for CPU parity. Existing staged RMSNorm parity and bounded C ABI pass with zero VVL errors; FFN semantics are not yet publicly wired and the FFN gate remains closed.
- Round 239 exercised the staged FFN forward primitive with deterministic input and the graph's initialized zero FFN weights/biases. CPU identity reference passes on RADV and lavapipe (`ffn=0`) with zero VVL errors; bounded C ABI also passes. This validates only the zero-parameter FFN seam and does not open the FFN/full-base gate.
- Round 240 restored deterministic nonzero FFN W1/B1/W2/B2 through the existing graph state API and compared GELU-residual output against a CPU double reference. RADV and lavapipe both pass (`ffn_nonzero=7.45058e-08`) with zero VVL errors; the optimizer step is preserved at 2. This closes a forward-kernel parity seam only; FFN public/full-chain gates remain closed.
- Round 241 added staged FFN upstream-gradient seeding, backward dispatch, and synchronized dx readback, then compared nonzero-parameter FFN backward against a CPU GELU derivative reference. RADV and lavapipe both pass with `ffn_dx=2.42144e-08` and zero VVL errors; bounded C ABI also passes. FFN parameter-gradient/full-chain integration remains outstanding and public gates stay closed.
- Round 242 added `readback_ffn_gradients_staged`, a synchronized bounded diagnostic seam for graph-owned W1/B1/W2/B2 gradients. Initial local-width naming errors were corrected before the successful build. Staged RMSNorm/FFN regressions and bounded C ABI pass with zero VVL errors; FFN gradient semantics are not yet parity-tested and public gates remain closed.
- Round 243 added CPU double-reference checks for all staged FFN W1/B1/W2/B2 gradients. After correcting the reference to accumulate B1 over all rows, RADV and lavapipe pass with `ffn_grad=7.45058e-09` (W1 `5.11591e-12`, B1 `1.45519e-10`, W2 `1.74623e-10`, B2 `7.45058e-09`) and zero VVL errors; bounded C ABI also passes. FFN remains staged-only and public gates stay closed.
- Round 244 added trailing-valid mask parity for all staged FFN parameter gradients. Masked CPU references pass on RADV and lavapipe (`masked_ffn_grad=1.45519e-10`) with zero VVL errors; bounded C ABI also passes. Middle-mask and public full-chain gates remain closed.
- Round 245 added all-masked FFN parameter-gradient coverage. All W1/B1/W2/B2 outputs are zero (`all_masked_ffn_grad=0`) on RADV and lavapipe with zero VVL errors; bounded C ABI passes. This confirms masked gradient clearing in the staged seam only; public FFN/full-base gates remain closed.
- Round 246 added the explicit `run_ffn_adamw_staged` API alias and one-step all-group W1/B1/W2/B2 AdamW parity after the two-step gamma update. The shared graph step advances from 2 to 3; CPU bias-corrected reference matches with `ffn_adamw=9.31323e-10` on RADV and lavapipe, zero VVL errors, and bounded C ABI pass. This remains staged optimizer evidence, not public training availability.
- Round 247 added FFN checkpoint restore/re-readback and oversized-step rejection coverage for weights, first/second moments, and the shared step. Both RADV and lavapipe pass with `ffn_restore=0`, `ffn_reject=0`, step `3`, and zero VVL errors; bounded C ABI passes. Full schema-v4 constructor/C ABI semantics and active-chain integration remain open.
- Round 248 extended `bounded_full_graph_cabi` to exercise the public C ABI FFN state readback after initialization, checking all serialized weights/moments and the step. RADV and lavapipe both pass with zero VVL errors; staged regression remains passing. This is C ABI state round-trip coverage, not schema-v4 constructor or active full-base training support.
- Round 249 extended the bounded C ABI test with null/undersized/oversized-step rejection checks for FFN update/readback and gamma update/readback. RADV and lavapipe pass with zero VVL errors, and the staged regression remains passing. These are fail-closed argument checks; schema-v4 constructor integration and public training gates remain closed.
- Round 250 extended the bounded C ABI test with schema-v4 checkpoint creation/readback metadata checks: all seven group bits, rank, shared step, normalization count, and FFN weight-state count. RADV and lavapipe both pass with zero VVL errors; staged regression remains passing. Checkpoint accessors are covered, but constructor semantics and active true-RMSNorm/full-chain training remain gated.
- Round 251 added fail-closed null handling checks for checkpoint metadata, weight, normalization-state, and FFN-state accessors. Bounded C ABI passes on RADV and lavapipe with zero VVL errors; staged regression remains passing. Version/profile/group mutation rejection is still not exposed through the opaque C ABI and active full-base training remains closed.
- Round 252 added invalid group and state-index checks for checkpoint C ABI accessors. Unknown groups and out-of-range normalization/FFN component/state indices return zero/null as required; bounded C ABI passes on RADV and lavapipe with zero VVL errors, and staged regression remains passing. Schema mutation and active-chain gates remain closed.
- Round 253 exercised opaque schema-v4 checkpoint update: read back a checkpoint, modified normalization gamma through its accessor, applied it through `spaceslug_tiny_forward_update_base_checkpoint`, and confirmed gamma readback changed to `1.125`. RADV and lavapipe pass with zero VVL errors; staged regression remains passing. This proves bounded checkpoint mutation/readback only; true normalization execution and public full-base gates remain closed.
- Round 254 added central finite-difference checks for one W1, B1, W2, and B2 element against the staged FFN objective `sum(output * dy)`. RADV and lavapipe pass with `ffn_fd` below `3.3e-7` and zero VVL errors; bounded C ABI also passes. This validates selected FFN parameter derivatives only; full-chain finite differences and active training remain gated.
- Round 255 repeated the selected FFN finite-difference checks with a trailing-valid half-window mask. RADV reports `masked_ffn_fd=2.12494e-07`, lavapipe `1.74397e-07`, both with zero VVL errors; bounded C ABI passes. This extends masked staged derivative evidence only; arbitrary mixed masks and full-chain gates remain closed.
- Round 256 added fail-closed invalid-input checks for staged FFN seed/readback, forward, backward, and parameter-gradient APIs (null pointers, non-whole-row counts, zero rows, and over-capacity rows). Both drivers pass with `invalid_ffn=0` and zero VVL errors; bounded C ABI passes. Public training remains gated.
- Round 257 made staged true-RMSNorm orchestration explicit by reseeding upstream `dy` before unmasked, trailing-masked, and all-masked state-chain runs. RADV and lavapipe pass the full staged RMSNorm/FFN suite with zero VVL errors; bounded C ABI also passes. This verifies diagnostic sequencing and barriers only; active primary graph integration remains open.
- Round 261 added staged FFN forward output parity for trailing-valid and all-masked masks. RADV and lavapipe pass with `masked_ffn_forward=7.45058e-08` and `all_masked_ffn_forward=0`, zero VVL errors, and bounded C ABI pass. The test restores the active mask before downstream gradients; public/full-chain gates remain closed.
- Round 262 added bounded C ABI assertions for non-null capability descriptions and explicitly closed retained-backward/optimizer and arbitrary-shape full-base status gates. RADV and lavapipe pass with zero VVL errors; staged regression remains passing. These assertions guard the current support boundary and do not open full-base training.
- Round 263 performed a clean debug configure/build and corrected the stale `gamma_forward_effect` expectation to assert the intentionally closed primary normalization gate (`gated max=0`). The full RADV CTest suite passes 89/89 after the correction. The long lavapipe full-suite run reached test 18 after 227 seconds before the command cap terminated it; a 44-test lavapipe training/FFN/RMSNorm subset passes 44/44, and fresh-build bounded/gamma/staged regressions pass with zero VVL errors. Full-base gates remain closed.
- Round 264 verified clean-build SPIR-V validity and optimization markers for `tiny_train_forward_rmsnorm`, `ffn_gelu_forward`, `ffn_gelu_backward`, and `ffn_param_grads_masked`: each validates with substantial arithmetic instruction counts (96, 15, 25, and 62 respectively). Fresh staged and bounded tests pass on both RADV and lavapipe with zero VVL errors; all public full-base/normalization/FFN/dataset/retained/arbitrary-shape gates remain false.
- Round 265 extended the intentionally gated `gamma_forward_effect` test to run staged state-only true RMSNorm and compare raw/inverse-RMS state against CPU reference (`staged_state_ref=2.28882e-05` RADV, `1.52588e-05` lavapipe). Primary legacy output remains unchanged (`gated max=0`), and gamma/staged/bounded tests pass with zero VVL errors. This is staged diagnostic parity, not active primary-graph integration.
- Round 266 added an explicit staged composition smoke: graph-owned true RMSNorm state is produced with an active trailing mask, read back under the full-capacity API contract, fed into the FFN primitive, and then the standalone FFN input is restored before existing nonzero-parameter checks. RADV and lavapipe staged tests pass with zero VVL errors; bounded C ABI remains passing. Gamma is synchronized with the restored nonzero-moment state to keep checkpoint assertions meaningful. This remains bounded orchestration, not active full-chain training.
- Round 267 added explicit capability assertions to the bounded orchestration test: constructor, normalization, FFN, dataset-resident, retained-backward/optimizer, and arbitrary-shape full-base gates must all remain false while the bounded position/gamma/FFN optimizer path runs. Three-step orchestration reduces loss from `22.2236` to `22.2235` and reaches shared gamma/FFN step 3 on both RADV and lavapipe, with zero VVL errors. Staged parity also remains passing; no public gate was opened.
- Round 268 expanded `test_training_gates` to instantiate the public graph and verify invalid FFN diagnostic inputs reject while the declared normalization optimizer entry point returns `trainable_normalization_unsupported` (`-5`). Gate strings/statuses remain consistent on RADV and lavapipe with zero VVL errors; bounded orchestration still passes. The FFN forward-loss helper is intentionally diagnostic/supported and is not treated as public FFN training capability.
- Round 269 strengthened bounded C ABI checkpoint coverage for schema-v4 normalization: checkpoint payload validation now requires the dedicated gamma moment-state accessors (state 1/2) in addition to group mask, shared step, profile rank, and float counts. RADV and lavapipe bounded checkpoint tests pass with zero VVL errors, and training-gate regression passes. Generic legacy AdamW accessors remain limited to legacy groups by design; no capability gate was opened.
- Round 270 expanded schema-v4 checkpoint coverage to require all four FFN components (W1, B1, W2, B2) across parameter, first-moment, and second-moment C ABI states. RADV and lavapipe bounded checkpoint tests and staged parity pass with zero VVL errors; public gate regression remains passing. This validates payload completeness only and does not expose public FFN training.
- Round 271 added malformed schema-v4 checkpoint rejection coverage through the C++ carrier used by the opaque C ABI: version 3, mismatched profile rank, and zero group mask are each rejected before device update. Schema-v4 nonzero-moment resume still passes (`normalization=64`, `ffn=33088`, `resume_step=1`, nonzero moments) on RADV and lavapipe with zero VVL errors; bounded C ABI remains passing.
- Round 272 added finite-value validation for every schema-v4 parameter and optimizer-state vector before checkpoint device mutation, plus tests for NaN normalization payload and `UINT64_MAX` shared AdamW step rejection. Schema-v4 checkpoint tests pass on RADV and lavapipe with zero VVL errors; bounded C ABI and public gate regressions remain passing. Full-base gates remain closed.
- Round 273 extended schema-v4 malformed-update checks to include infinity in an FFN parameter and verified rejected NaN/infinity/metadata/step updates preserve the pre-update gamma and FFN payload snapshots. RADV and lavapipe schema-v4 tests pass with zero VVL errors; bounded C ABI remains passing. This strengthens fail-closed atomicity without opening any full-base gate.
- Round 274 added explicit dataset-resident full-training, retained-backward/optimizer, and arbitrary-shape gate assertions. The public dataset C ABI wrapper rejects null opaque batches, while compile-time status remains `dataset_training_full_unsupported=-4`. RADV and lavapipe bounded C ABI and training-gates tests pass with zero VVL errors; no unsupported path was widened.
- Round 275 expanded FFN C ABI boundary coverage to assert null graph rejection for gamma/FFN state, checkpoint, dataset-full, and bounded-training wrappers, matching their actual invalid-argument conventions (checkpoint/dataset/bounded wrappers return `1`; state wrappers return `-1`). RADV and lavapipe C ABI gate tests pass with zero VVL errors, bounded C ABI remains passing, and all full-base gates stay closed.
- Round 276 expanded profile metadata coverage to reject out-of-range base group IDs and unsupported LoRA ranks while retaining rank-4/rank-8 support assertions. Metadata runs without Vulkan initialization and passes on both RADV and lavapipe; training-gate and bounded orchestration regressions pass with zero VVL errors. No capability gate was widened.
- Round 277 added explicit staged FFN mask rejection checks for values greater than one and middle holes, alongside existing trailing-valid and all-masked forward/gradient parity. RADV and lavapipe staged tests pass with zero VVL errors; bounded C ABI remains passing. Masking remains trailing-valid only and does not imply arbitrary mixed-mask full-base support.
- Round 278 added partial-row and all-masked state-only RMSNorm forward parity. Both cases compare against the CPU RMSNorm state formula (`partial_state=1.19209e-07`; `all_masked_state=1.19209e-07` RADV and lavapipe), clarifying that forward normalization computes state while masks govern eligible backward/FFN rows. Staged and bounded orchestration regressions pass with zero VVL errors; full-base gates remain closed.
- Round 279 explicitly checked the semantic split between active paths: primary legacy forward remains gamma-invariant (`gated max=0`), while staged true RMSNorm state applies gamma with zero measured error (`staged_gamma_effect=0`) and preserves CPU raw/inverse-RMS parity. RADV and lavapipe gamma tests, bounded C ABI, and staged RMSNorm/FFN regressions pass with zero VVL errors. This is evidence for eventual atomic integration, not a gate opening.
- Round 280 added composed staged backward coverage: an FFN output gradient is propagated through the staged FFN and then true RMSNorm backward, with CPU reference error `composed_norm_dx=1.19209e-07` on both drivers. The test restores standalone FFN input and nonzero-moment gamma state before existing gradient/checkpoint checks; RADV and lavapipe staged plus bounded C ABI tests pass with zero VVL errors. Downstream position/embedding and active loss-chain integration remain gated.
- Round 281 explicitly pinned the active/staged backward boundary by asserting unsupported normalization/FFN error codes (`-5`/`-6`) alongside the existing full-base gate strings. RADV and lavapipe combined-training gating and staged composed-backward tests pass with zero VVL errors; bounded orchestration remains passing. Active true-RMSNorm backward is still not exposed.
- Round 282 expanded schema-v4 checkpoint C ABI accessor tests for invalid group IDs, invalid FFN component/state indices, invalid normalization state indices, and invalid group counts. Valid schema-v4 resume and nonzero-moment checks remain passing on RADV and lavapipe, FFN C ABI boundary and bounded checkpoint tests pass with zero VVL errors. Full-base gates remain closed.
- Round 283 tightened staged gamma and all-group FFN AdamW validation to reject negative weight decay before consuming the shared optimizer step, and added nonfinite learning-rate coverage. Initial test exposure found the gamma path accepted negative decay; the API validation was corrected, then RADV and lavapipe staged optimizer parity passed with zero VVL errors and unchanged step counts. Bounded orchestration remains passing; no full-base gate was widened.
- Round 284 revalidated schema-v4 checkpoint payload atomicity after the optimizer validation change. Valid checkpoint creation, C ABI readback, nonzero-moment resume, malformed-payload rejection, and staged optimizer parity pass on RADV and lavapipe with zero VVL errors; rejected updates preserve graph state. Full-base normalization/FFN integration and constructor gates remain closed. Signed-zero and extreme-finite payload acceptance were also added and verified, with baseline state restored before the atomicity assertion.
- Round 285 re-ran the bounded full-graph C ABI boundary suite after the staged optimizer validation updates. Null checkpoint/state handles, invalid group/state indices, middle-mask holes, zero/oversized rows, and zero-rate optimizer inputs remain rejected without changing the optimizer step; valid bounded training and all-masked metrics remain passing on RADV and lavapipe with zero VVL errors. The public C ABI remains bounded and fail-closed; no full-base gate was widened.
- Round 286 added explicit bounded C ABI rejection checks for nonfinite learning rate and negative weight decay. RADV and lavapipe bounded full-graph C ABI tests pass with zero VVL errors, and valid training loss/step behavior remains unchanged. Dataset-resident, retained-backward, arbitrary-shape, and active true-RMSNorm paths remain gated.
- Round 287 audited stale suite wording and replaced it with explicit current-evidence language. The focused current set—`training_gates`, `combined_training_gating`, schema-v4 `base_checkpoint_cabi`, `tiny_rmsnorm_staged_state`, and `bounded_training_orchestration`—passes on both RADV and lavapipe with zero VVL errors. Results retain `constructor=0`, `normalization=0`, `ffn=0`, `dataset_full=0`, `retained_backward_optimizer=0`, and `arbitrary_shape_full_base=0`; this audit does not open any gate.
- Round 288 pinned exact retained-backward and arbitrary-shape public capability strings in `test_training_gates`, in addition to existing boolean and dataset capability assertions. RADV and lavapipe training-gate, combined-gating, and profile metadata tests pass with zero VVL errors; capability metadata remains fail-closed and no gate was widened.
- Round 289 synchronized `SCHEMA_V4_PAYLOAD_DESIGN.md`, `FFN_GRAPH_STATE_DESIGN.md`, and `FFN_NORMALIZATION_CONTRACT.md` with the implemented graph-owned staged state, schema-v4 C ABI, finite-payload/continuation evidence, and the still-closed active-chain gates. RADV and lavapipe checkpoint, staged parity, gate, and bounded orchestration tests pass with zero VVL errors; documentation changes are whitespace-clean.
- Round 290 extended profile coverage to instantiate the runtime graph with supported rank-4 and rank-8 constructors and assert the selected profile descriptor. RADV and lavapipe profile metadata/constructor tests and RADV training-gate regression pass with zero VVL errors. Unsupported full-base constructor semantics remain fail-closed; no capability gate was widened.
- Round 291 extended the constructor boundary with the C ABI `create_rank` invalid-rank/null-input rejection assertion. RADV and lavapipe profile/constructor tests and RADV training-gate regression pass with zero VVL errors; supported profile selection remains rank-4/rank-8 only. The public full-base constructor gate remains closed.
- Round 292 strengthened `test_training_gates` so the full-base capability string must explicitly mention gamma/FFN, schema-v4 C ABI, dataset, retained, and arbitrary-shape gates. RADV and lavapipe gate tests plus lavapipe schema-v4 checkpoint regression pass with zero VVL errors; capability wording remains fail-closed and no gate was widened.
- Round 293 ran the complete current training-focused CTest inventory (46 tests selected from the build) on both drivers with validation enabled: RADV `46/46` passed in 4.33 seconds and lavapipe `46/46` passed in 319.58 seconds. The inventory includes combined gradients/continuation, checkpoint/C ABI, masked and staged RMSNorm/FFN, optimizer/finite differences, bounded orchestration, legacy base groups, LoRA, and profile tests. This broad regression evidence still does not satisfy the explicitly incomplete active-chain, retained, dataset-resident, arbitrary-shape, and synchronization-clean gates.
- Round 294 completed the full RADV CTest lifecycle at `89/89` with validation enabled and zero VVL errors. The lavapipe lifecycle was intentionally time-capped after 18/89 tests because its combined-loss trace alone took 226.58 seconds; all 18 reached before the cap passed, including the changed training/checkpoint/gate/orchestration coverage. Full lavapipe lifecycle completion remains outstanding evidence and no gate was widened.
- Round 295 completed the remaining lavapipe lifecycle inventory (CTest indices 19–89) at `71/71` passed in 409.63 seconds with validation enabled and zero VVL errors. Together with Round 294's first 18 passing tests, this supplies complete `89/89` lavapipe lifecycle coverage. Changed staged RMSNorm/FFN and both-driver training-gate regressions also pass; active-chain, retained, dataset-resident, arbitrary-shape, and public synchronization-clean gates remain explicitly closed.
- Round 296 tied the combined CPU gradient/readback parity test to the closed active normalization, FFN, and full-base constructor capability flags. Combined gradient/optimizer parity passes on RADV and lavapipe with zero VVL errors (`max_ffn_gradient_error=0`, `max_gamma_gradient_error=0`); full-base gate regression also passes. This remains evidence for staged components, not active-chain exposure.
- Round 297 added final explicit public-gate assertions for the dataset unsupported code (`-4`), retained-backward status, and arbitrary-shape status in combined gating. RADV and lavapipe combined gate tests pass with zero VVL errors, and bounded orchestration remains passing. Audit confirms the safe next increment is a staged true-RMSNorm-`rmsDx_` handoff to position/embedding consumers; direct active-pipeline swapping is unsafe because normalization placement, liveness, barriers, and loss-chain semantics remain unresolved.
- Round 298 verified the already-embedded staged true-RMSNorm embedding-gradient consumer on RADV and lavapipe (`embedding_training_rmsnorm_dx PASS max_error=0`, zero VVL errors). The intended position-gradient consumer shader is embedded but has no standalone executable target yet; no unverified position handoff was claimed and active public gates remain closed.
- Round 299 added the standalone `position_training_rmsnorm_dx` test and CTest registration for the embedded true-RMSNorm position consumer. Its trailing/all-masked row CPU parity passes on RADV and lavapipe with `max_error=0` and zero VVL errors; combined training gating also passes. This validates the staged handoff primitive only—active position/embedding rewiring and full-base exposure remain gated.
- Round 300 performed final dual-driver verification of the changed evidence set: position dx consumer, staged RMSNorm/FFN parity (including composed backward and optimizer), schema-v4 checkpoint C ABI, training gates, combined gates, and bounded orchestration all pass on RADV and lavapipe with zero VVL errors. Gate output remains `constructor=0 normalization=0 ffn=0 dataset_full=0 retained_backward_optimizer=0 arbitrary_shape_full_base=0`; therefore the objective is not complete and the goal remains active for future atomic active-chain integration.
- Round 301 added a runtime constructor-time presence check for both embedded staged true-RMSNorm downstream gradient shaders, without selecting them in the active legacy chain. RADV and lavapipe profile, gate, position-consumer, and staged RMSNorm/FFN tests pass with zero VVL errors and `max_error=0` for the position consumer. Active integration, dataset-resident, retained, arbitrary-shape, and public synchronization gates remain closed.
- Round 302 made the staged handoff boundary explicit in runtime comments: both true-dx consumers are presence-checked but deliberately not bound to the legacy active chain until placement, liveness, synchronization, and end-to-end parity gates complete. Position consumer parity passes on RADV and lavapipe (`max_error=0`) with zero VVL errors, and combined training gating remains passing with full-base disabled.
- Round 303 verified CTest registration for both staged downstream consumers (`embedding_training_rmsnorm_dx`, `position_training_rmsnorm_dx`) and reran position consumer, staged RMSNorm/FFN parity, training-gate, and bounded C ABI tests on RADV and lavapipe. All pass with zero VVL errors; gate output remains all false (`constructor`, `normalization`, `ffn`, `dataset_full`, `retained_backward_optimizer`, `arbitrary_shape_full_base`).
- Round 304 corrected a destructor cleanup regression exposed by validation: the embedding SGD shader had been double-destroyed while the position SGD shader was destroyed under the wrong handle. Cleanup now destroys each shader exactly once after its matching resources; RADV and lavapipe gate/profile tests pass with zero VVL errors. This is lifecycle hygiene only and does not alter public training gates.
- Round 305 corrected stale documentation and completed a fresh full CTest regression after the cleanup fix: RADV `90/90` and lavapipe `90/90` passed with validation enabled and zero VVL errors. The roadmap now reflects the current staged baseline and no longer claims a known residual staging hazard; integrated active-chain synchronization remains an unaccepted gate because that chain is not yet exercised end to end.
- Round 306 corrected the remaining reverse-order cleanup issue for `positionGradientShader_`: its pipeline, layout, descriptor set layout, and pool are now torn down before the shader module. The targeted profile, gate, and position-consumer CTests pass on RADV and lavapipe with zero VVL errors. Public gates remain closed.
- Round 307 completed the cleanup audit: `positionSgdShader_` is now destroyed in its own reverse-order resource block, with no duplicate destruction. RADV and lavapipe training-gate runs pass with zero VVL errors, and the full-suite evidence remains `90/90` on each driver. This does not change active-chain capability gates.
- Round 308 strengthened combined-training continuation coverage: invalid null, negative-learning-rate, negative-decay, and NaN-learning-rate calls are rejected without changing gamma/FFN state or the shared step. The test passes on RADV and lavapipe with zero VVL errors; valid two-step continuation remains passing and all public full-base gates remain closed.
- Round 309 audited shared-step atomicity. The normal fixed-window AdamW path now computes the next step but defers mutating `adamwStep_` until staging memory is successfully mapped; recorded push constants use that validated next step. RADV and lavapipe continuation regressions pass with zero VVL errors. An exploratory trailing-mask change in the diagnostic dstate helper was rejected by its existing test contract and fully reverted; no unsupported behavior was introduced.
- Round 310 completed standalone C ABI AdamW hardening: wrapper and graph validate finite learning rate/betas/epsilon, nonnegative decay, nonzero bounded rows, and defer step mutation until staging succeeds. Added no-mutation tests for invalid optimizer calls. Corrected standalone LM-head gradient/SGD/AdamW push-constant ranges to match shader blocks; standalone training and checkpoint parity now pass on RADV and lavapipe with zero VVL errors.
- Round 312 extended the standalone C ABI rejection matrix with null activation/gradient pointers and zero-row calls, confirming all reject before state access. The full tiny-base training and checkpoint parity test passes on RADV and lavapipe with zero VVL errors; public full-base capabilities remain unchanged and gated.
- Round 313 hardened standalone AdamW checkpoint restore: oversized steps and non-finite weight/moment payloads are rejected before mapping or device mutation. Added regression coverage; standalone training/checkpoint parity passes on RADV and lavapipe with zero VVL errors. This remains a bounded standalone ABI improvement and does not widen full-base capability.
- Round 314 hardened the bounded full-graph C ABI wrapper with preflight validation for null inputs, zero/out-of-profile rows, non-finite optimizer values, invalid beta/epsilon ranges, and negative decay. Expanded C ABI tests cover null-token and zero-row rejection; bounded full-graph tests pass on RADV and lavapipe with zero VVL errors. The active full-base gates remain closed.
- Round 311 audited the bounded graph's public entry-point contracts and confirmed that the legacy `train_positions_adamw` path remains explicitly scale-only despite its integrated gamma/FFN state updates. No unsafe active-chain swap was made; the source, staged shaders, and documented placement boundary remain aligned. Existing complete-suite evidence is unchanged at `90/90` on each driver with zero VVL errors, and all full-base gates remain closed.
- Round 258 added a nonzero-moment second all-group FFN AdamW step. CPU bias-corrected parity passes on RADV and lavapipe (`ffn_adamw_two=9.31323e-10`), with the shared step advancing to 4 and zero VVL errors; bounded C ABI passes. FFN optimizer coverage remains staged-only and public gates stay closed.
- Round 259 added explicit null checkpoint pointer rejection coverage for opaque C ABI readback/update wrappers while preserving valid checkpoint accessor tests. Bounded C ABI passes on RADV and lavapipe with zero VVL errors; staged regression remains passing. Schema version/profile/group mutation and active full-base semantics remain gated.
- Round 260 verified that rejected null checkpoint readback/update wrapper calls leave the previously read gamma state and shared step unchanged. Bounded C ABI passes on RADV and lavapipe with zero VVL errors; staged regression remains passing. This is wrapper-level state-preservation evidence and does not open active full-base training.
- Round 158 final dual-driver functional regressions pass for metrics parity, combined gradient/optimizer parity, bounded orchestration, capability gates, schema-v4 C ABI, and validation-enabled capability setup.
- Historical focused-suite counts above are retained for provenance; the current staged/capability/checkpoint/orchestration evidence is validation-clean on both RADV and lavapipe. Complete 89-test dual-driver lifecycle coverage is recorded in Rounds 294-295, but it does not by itself satisfy the separate integrated active-chain synchronization or public full-base gates.
- Public capabilities remain fail-closed; dataset-resident full training remains explicitly unsupported without host fallback.
- Packed FFN state layout, constructed-graph round-trip, native schema-v4 checkpoint/restore, and C ABI bindings.

## Explicitly not complete

The bounded orchestration smoke test proves repeated graph-owned optimizer submissions, and the active fixed-profile post-attention true-RMSNorm/FFN chain passes focused CPU-gradient, continuation, loss-decrease, schema-v4/C ABI, masked, and validation-enabled RADV/lavapipe evidence. It is not evidence for long-horizon convergence or general full-base availability. The bounded capability is exposed only for the fixed Tiny profile; retained and dataset-resident full training remain unsupported. Longer-horizon parity through the complete attention/projection chain, arbitrary mixed-mask attention, retained backward/optimizer execution, dataset-resident execution, and arbitrary-shape full-base ABI remain gated. The general full-base capability remains fail-closed.

The following remain gated:

- synchronization-clean full integrated lifecycle beyond the focused bounded validation suite;
- longer-horizon CPU parity and practical loss-trace acceptance;
- primary mask-aware attention for arbitrary mixed masks;
- complete normalization forward/backward contract;
- retained backward/optimizer execution;
- dataset-resident full training;
- arbitrary-shape full-base ABI.

No host per-token fallback is implied by these gates.
