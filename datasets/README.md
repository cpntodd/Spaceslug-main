# Dataset workspace

- `manifests/` — committed dataset descriptors and retrieval metadata.
- `samples/` — small, license-compatible fixtures for tests and documentation.
- Published `.dts` bundles are external artifacts referenced by checksum.

Protected benchmark definitions belong in `benchmarks/`. Do not place private or unlicensed source data here.
