# Benchmarks

This directory contains immutable benchmark definitions, manifests, and retrieval metadata. Benchmark data must not be silently regenerated under an existing identifier or included in training datasets.

Every benchmark result must reference the exact benchmark revision, model, tokenizer, code, hardware, backend, and metric definition.
